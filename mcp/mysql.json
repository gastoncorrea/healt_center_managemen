{
  "sources": {
    "mysql-db": {
      "kind": "mysql",
      "host": "${MYSQL_HOST}",
      "port": 3306,
      "database": "${MYSQL_NAME}",
      "user": "${MYSQL_USER}",
      "password": "${MYSQL_PASSWORD}",
      "queryTimeout": "30s"
    }
  }
}
